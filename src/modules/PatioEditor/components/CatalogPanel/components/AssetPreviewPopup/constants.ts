/** Gap between the sidebar's right edge and the popup. */
export const PREVIEW_SIDE_OFFSET_PX = 8;

/**
 * Distance from the sidebar's top to the popup's top. Fixed so the popup sits at
 * the same spot regardless of which asset (or scroll position) opened it.
 */
export const PREVIEW_TOP_OFFSET_PX = 186;

/** How long the copy-link button shows its confirmed checkmark before reverting. */
export const COPIED_RESET_MS = 2000;
