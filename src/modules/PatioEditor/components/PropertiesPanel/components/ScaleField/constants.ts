// Scale is stored on the object as a uniform ratio (1 = 100%); this field is a
// percent view over it. Presets and the min/max clamp are expressed in percent.
export const SCALE_PRESETS = [25, 50, 75, 100, 200];

export const SCALE_MIN_PERCENT = 1;
export const SCALE_MAX_PERCENT = 1000;

export const PERCENT_PER_RATIO = 100;
